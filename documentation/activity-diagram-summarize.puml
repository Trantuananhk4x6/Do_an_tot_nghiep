@startuml activity-diagram-summarize
title Activity Diagram - Summarize (Phân tích CV chuyên sâu)
skinparam backgroundColor #FEFEFE
skinparam ActivityBackgroundColor #E8F4FD
skinparam ActivityBorderColor #3498DB
skinparam ActivityStartColor #27AE60
skinparam ActivityEndColor #E74C3C
skinparam ActivityDiamondBackgroundColor #F39C12
skinparam ActivityDiamondBorderColor #E67E22
skinparam SwimlaneBorderColor #2C3E50
skinparam SwimlaneTitleBackgroundColor #34495E
skinparam SwimlaneTitleFontColor white

|User|
start
:Chọn "Summarize" trên menu;

|System|
:Hiển thị giao diện Summarize;
:Hiển thị Language Selector;
:Hiển thị khu vực Upload file;

|User|
:Chọn ngôn ngữ phân tích (VI/EN/JA/ZH/KO);
:Upload file CV (PDF/DOCX/TXT/Audio);

|System|
:Hiển thị file đã upload;

|User|
:Nhấn nút "Phân tích CV";

|System|
:Gửi file đến AI Backend;
:Hiển thị loading "Đang phân tích CV...";

if (Phân tích thành công?) then (Thành công)
  :Nhận kết quả từ AI;
  
  :Hiển thị Điểm CV tổng thể (0-100);
  
  :Hiển thị Tóm tắt tổng quan:
  - Số từ
  - Thời gian đọc;
  
  :Hiển thị Phân tích kỹ năng:
  - Technical Skills
  - Soft Skills
  - Languages
  - Tools;
  
  :Hiển thị Điểm chính (Key Points);
  
  :Hiển thị Điểm yếu cần cải thiện;
  
  :Hiển thị Gợi ý nghề nghiệp phù hợp;
  
  :Hiển thị Việc cần làm (Action Items);
  
else (Lỗi)
  if (Rate limit?) then (Có)
    :Hiển thị thông báo quá tải;
    :Yêu cầu thử lại sau;
  else (Không)
    :Hiển thị thông báo lỗi;
    :Gợi ý upload file khác;
  endif
endif

|User|
:Xem kết quả phân tích;

if (Muốn phân tích file khác?) then (Có)
  :Upload file mới;
  |System|
  :Reset kết quả cũ;
  :Quay lại bước phân tích;
else (Không)
  :Hoàn thành;
endif

stop

legend right
  |= Ký hiệu |= Mô tả |
  | Hình oval | Bắt đầu/Kết thúc |
  | Hình chữ nhật | Hoạt động |
  | Hình thoi | Điều kiện |
  | Swimlane | Phân chia Actor |
endlegend

@enduml
